import React from "react";

import styled from "styled-components";

const StyledTestTitle = styled.div``;

function Home() {
  return <StyledTestTitle>Home</StyledTestTitle>;
}

export default Home;
